# Validation Results — placeholder

Status: not yet written. Will report, separately and without conflating them:
1. Quantitative metrics (accuracy, macro F1, per-class P/R/F1, confusion
   matrices) on public-benchmark-derived validation data (synthetic
   noise/quality mixes + mapped speech-emotion corpora).
2. A small, explicitly-caveated leave-one-out sanity check on the 3 labeled
   production calls (directional only — n=3 has no statistical power).
